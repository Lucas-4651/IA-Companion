<!DOCTYPE html>
<html lang="fr" data-theme="dark">
<head>
<<<<<<< HEAD
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IA-Companion </title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
=======
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>IA-Companion </title>
  <meta name="description" content="Guide IA gratuit : catalogue d'outils, générateur de prompts et conseils.">
  <link rel="manifest" href="/manifest.json">
  <meta name="theme-color" content="#0891b2">
  <meta property="og:title" content="IA-Companion">
  <meta property="og:description" content="Votre guide IA gratuit">
  <meta property="og:image" content="https://ton-domaine.com/og.jpg">
  <meta name="monetization" content="$ilp.uphold.com/ton-compte">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <script src="https://unpkg.com/@lottiefiles/lottie-player@latest/dist/lottie-player.js"></script>
  <style>
    :root {
      --primary: #0891b2;
      --secondary: #3b82f6;
      --bg-dark: #0f172a;
      --bg-light: #f8fafc;
      --text-dark: #0f172a;
      --text-light: #f1f5f9;
      --border: rgba(6,182,212,.4);
    }
    *{margin:0;padding:0;box-sizing:border-box;font-family:'Segoe UI',Tahoma,Geneva,Verdana,sans-serif}
    body{background:var(--bg-dark);color:var(--text-light);min-height:100vh;display:flex;flex-direction:column;padding-bottom:120px;transition:background .3s,color .3s}
    [data-theme="light"]{--bg-dark:var(--bg-light);--text-light:var(--text-dark);--border:rgba(6,182,212,.8)}
    .container{max-width:1200px;margin:0 auto;padding:2rem;flex:1}
    .skip-link{position:absolute;top:-40px;left:6px;background:var(--primary);color:#fff;padding:8px;z-index:100;border-radius:4px}.skip-link:focus{top:6px}
    header{text-align:center;padding:3rem 0}
    h1{font-size:2.5rem;margin-bottom:1rem;background:linear-gradient(90deg,var(--primary),var(--secondary));-webkit-background-clip:text;background-clip:text;color:transparent;font-weight:700}
    .features{display:grid;grid-template-columns:repeat(auto-fit,minmax(250px,1fr));gap:2rem;margin:3rem 0}
    .feature-card{background:rgba(30,41,59,.7);border-radius:12px;padding:1.2rem;border:1px solid rgba(99,102,241,.2);backdrop-filter:blur(8px);transition:transform .3s ease,box-shadow .3s ease}
    .feature-card:hover{transform:translateY(-5px);border-color:var(--primary);box-shadow:0 8px 20px rgba(6,182,212,.15)}
    .feature-card i{font-size:2.2rem;color:var(--primary);margin-bottom:.8rem}
    .feature-card h3{font-size:1.3rem;margin-bottom:.6rem;color:var(--text-light)}
    .feature-card p{color:#94a3b8;line-height:1.6;font-size:.95rem}
>>>>>>> 0edccb1 (Prod 🔥)

    /* Footer */
    footer{position:relative;overflow:hidden;background:linear-gradient(to top,var(--bg-dark) 0%,#1e293b 100%);color:var(--text-light);padding:3rem 1rem 1rem;border-top:1px solid var(--border);box-shadow:0 -6px 25px rgba(6,182,212,.15)}
    footer::before{content:"";position:absolute;inset:0;background-image:linear-gradient(rgba(6,182,212,.08) 1px,transparent 1px),linear-gradient(90deg,rgba(6,182,212,.08) 1px,transparent 1px);background-size:40px 40px;animation:gridMove 20s linear infinite}
    @keyframes gridMove{to{transform:translate(40px,40px)}}
    .footer-container{max-width:1200px;margin:0 auto;position:relative;z-index:1}
    .footer-content{display:grid;grid-template-columns:1fr 1fr;gap:2rem;margin-bottom:2rem}
    .footer-section h3,.footer-section h4{position:relative;display:inline-block;margin-bottom:1rem;color:var(--primary);letter-spacing:.5px}
    .footer-section h3::after,.footer-section h4::after{content:"";position:absolute;bottom:-6px;left:0;width:100%;height:2px;background:linear-gradient(90deg,var(--primary),var(--secondary));transform-origin:left;animation:neon 2s ease-in-out infinite alternate}
    .donation-text{font-size:.95rem;line-height:1.7;margin-bottom:1.2rem}
    .donate-btn{background:linear-gradient(90deg,var(--primary),#0ea5e9);color:#fff;border:none;padding:.9rem 1.7rem;border-radius:50px;font-weight:700;cursor:pointer;min-height:44px;transition:.3s ease;box-shadow:0 0 0 2px rgba(6,182,212,.3),0 0 15px rgba(6,182,212,.4);display:flex;align-items:center;gap:.5rem}
    .donate-btn:hover{transform:translateY(-3px) scale(1.05)}
    .footer-bottom{display:flex;flex-direction:column;align-items:center;gap:1rem;text-align:center;font-size:.85rem;color:#94a3b8;padding-top:1.5rem;border-top:1px solid rgba(99,102,241,.15)}
    .copyright{display:flex;flex-wrap:wrap;justify-content:center;gap:1rem}
    .social-links{display:flex;gap:1rem;flex-wrap:wrap;align-items:center}
    .social-btn{display:inline-flex;align-items:center;gap:.5rem;padding:.5rem 1rem;border-radius:8px;color:#cbd5e1;background:rgba(30,41,59,.5);border:1px solid rgba(6,182,212,.2);min-height:44px;transition:.3s ease}
    .social-btn:hover{color:var(--primary);border-color:var(--primary);box-shadow:0 0 12px rgba(6,182,212,.5)}
    .mail-link{color:var(--primary);text-decoration:none;transition:.3s ease}.mail-link:hover{color:var(--secondary)}
    @media (max-width:600px){.footer-content{grid-template-columns:1fr;gap:1.5rem}.copyright{flex-direction:column;gap:.5rem}.social-links{justify-content:center}}

    /* Toast centré */
    .donate-toast{position:fixed;inset:0;display:flex;align-items:center;justify-content:center;z-index:9999;background:rgba(0,0,0,.75);backdrop-filter:blur(8px);opacity:0;visibility:hidden;transition:opacity .4s,visibility .4s}
    .donate-toast.show{opacity:1;visibility:visible}
    .donate-toast__content{background:var(--bg-dark);color:var(--text-light);padding:2rem 3rem;border-radius:16px;box-shadow:0 0 30px rgba(6,182,212,.6);text-align:center;animation:pop .5s cubic-bezier(.68,-.55,.27,1.55) forwards}
    @keyframes pop{0%{transform:scale(.5) rotate(-5deg)}100%{transform:scale(1) rotate(0)}}
    .toast-close{position:absolute;top:.75rem;right:.75rem;background:none;border:none;color:var(--primary);font-size:1.2rem;cursor:pointer}
    #mvolaToast{cursor:pointer;user-select:all}
  </style>
</head>
<body>
  <a href="#main" class="skip-link">Aller au contenu</a>
  <div class="container" id="main">
    <header><h1>IA-Companion</h1></header>
    <div class="features">
      <div class="feature-card"><i class="fas fa-robot"></i><h3>Catalogue IA</h3><p>Découvrez notre sélection des meilleurs outils d'IA classés par usage.</p></div>
      <div class="feature-card"><i class="fas fa-lightbulb"></i><h3>Générateur de Prompts</h3><p>Créez des prompts efficaces en quelques clics.</p></div>
      <div class="feature-card"><i class="fas fa-graduation-cap"></i><h3>Astuces & Conseils</h3><p>Guides pratiques pour booster votre productivité.</p></div>
    </div>
  </div>

  <footer>
    <div class="footer-container">
      <div class="footer-content">
        <div class="footer-section">
          <h3>IA-Companion</h3>
          <p>Votre guide expert pour maîtriser les intelligences artificielles et transformer votre productivité.</p>
        </div>
        <div class="footer-section">
          <h4>Soutenez le projet</h4>
          <p class="donation-text">Ce site reste gratuit grâce à vous.<br><small>Un petit don ? chaque sourire compte ❤️</small></p>
          <button class="donate-btn" id="donateBtn"><i class="fas fa-hand-holding-heart"></i> Faire un don</button>
        </div>
      </div>
      <div class="footer-bottom">
        <div class="copyright">
          <span>&copy; <span id="currentYear"></span> IA-Companion</span>
          <span>Dev : Lucas46 Modder Madagascar</span>
          <span><a href="mailto:Rakotonirinalucas26@gmail.com" class="mail-link"><i class="fas fa-paper-plane"></i> Mail</a></span>
        </div>
      </div>
    </div>
  </footer>

  <!-- Toast centré animé -->
  <div id="donateToast" class="donate-toast" role="alert" aria-live="assertive">
    <div class="donate-toast__content">
      <lottie-player src="https://assets3.lottiefiles.com/packages/lf20_u4yrau.json" background="transparent" speed="1" style="width:80px;height:80px" autoplay loop></lottie-player>
      <p>Merci ❤️ MVola&nbsp;: <strong id="mvolaToast">038 70 85 243</strong></p>
      <button class="toast-close" aria-label="Fermer"><i class="fas fa-times"></i></button>
    </div>
  </div>

  <script>
    document.getElementById('currentYear').textContent = new Date().getFullYear();

    function showDonateToast() {
      document.getElementById('donateToast').classList.add('show');
      document.body.style.overflow = 'hidden';
    }
    function hideDonateToast() {
      document.getElementById('donateToast').classList.remove('show');
      document.body.style.overflow = '';
    }

    document.getElementById('donateBtn').addEventListener('click', showDonateToast);
    document.querySelector('.toast-close').addEventListener('click', hideDonateToast);
    document.getElementById('donateToast').addEventListener('click', e => {
      if (e.target === e.currentTarget) hideDonateToast();
    });
    document.getElementById('mvolaToast').addEventListener('click', () => {
      navigator.clipboard.writeText('0387085243').then(() => {
        const el = document.getElementById('mvolaToast');
        const old = el.textContent;
        el.textContent = 'Copié !';
        setTimeout(() => el.textContent = old, 1500);
      });
    });
  </script>
</body>
</html>
