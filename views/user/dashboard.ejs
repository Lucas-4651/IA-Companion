<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tableau de bord professionnel</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #0f172a;
            --secondary: #1e293b;
            --accent: #0ea5e9;
            --accent-light: #38bdf8;
            --text: #e2e8f0;
            --text-light: #94a3b8;
            --card-bg: rgba(30, 41, 59, 0.7);
            --border: rgba(148, 163, 184, 0.2);
            --neon-glow: 0 0 10px rgba(56, 189, 248, 0.3);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        body {
            background: linear-gradient(135deg, #0f172a, #1e293b);
            color: var(--text);
            min-height: 100vh;
            padding: 15px;
            font-size: 14px;
        }

        .dashboard-container {
            max-width: 100%;
            margin: 0 auto;
        }

        /* Header */
        .dashboard-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
            margin-bottom: 20px;
            border-bottom: 1px solid var(--border);
        }

        .header-left h1 {
            font-size: 1.4rem;
            font-weight: 600;
            background: linear-gradient(90deg, var(--accent-light), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            position: relative;
        }

        .header-left h1::after {
            content: '';
            position: absolute;
            bottom: -3px;
            left: 0;
            width: 35px;
            height: 2px;
            background: var(--accent);
            border-radius: 3px;
        }

        .user-profile {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .avatar {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            background: linear-gradient(45deg, var(--accent), #7dd3fc);
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 1rem;
            color: white;
        }

        .user-info {
            display: flex;
            flex-direction: column;
        }

        .user-name {
            font-weight: 600;
            font-size: 0.85rem;
        }

        .user-role {
            font-size: 0.7rem;
            color: var(--text-light);
        }

        /* Main Grid */
        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
            gap: 15px;
            margin-bottom: 20px;
        }

        .card {
            background: var(--card-bg);
            border: 1px solid var(--border);
            border-radius: 10px;
            padding: 18px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.15);
            transition: all 0.3s ease;
            backdrop-filter: blur(8px);
            min-height: 180px;
        }

        .card:hover {
            transform: translateY(-3px);
            box-shadow: var(--neon-glow), 0 6px 15px rgba(0, 0, 0, 0.2);
        }

        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 12px;
            padding-bottom: 10px;
            border-bottom: 1px solid var(--border);
        }

        .card-title {
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--text);
        }

        .card-icon {
            width: 32px;
            height: 32px;
            border-radius: 8px;
            background: rgba(14, 165, 233, 0.15);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--accent);
            font-size: 1rem;
        }

        /* Stats Cards */
        .stat-card {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100%;
            text-align: center;
        }

        .stat-value {
            font-size: 2.2rem;
            font-weight: 700;
            margin: 12px 0;
            background: linear-gradient(90deg, var(--accent-light), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .stat-label {
            font-size: 0.9rem;
            color: var(--text-light);
        }

        /* Quick Actions */
        .quick-actions-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 12px;
        }

        .quick-action {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 15px 10px;
            border-radius: 8px;
            background: rgba(30, 41, 59, 0.5);
            border: 1px solid var(--border);
            transition: all 0.2s ease;
            cursor: pointer;
            text-align: center;
            text-decoration: none;
            color: var(--text);
            min-height: 90px;
        }

        .quick-action:hover {
            background: rgba(14, 165, 233, 0.1);
            transform: translateY(-2px);
            border-color: var(--accent);
        }

        .action-icon {
            font-size: 1.4rem;
            margin-bottom: 6px;
            color: var(--accent);
        }

        .action-label {
            font-size: 0.75rem;
            font-weight: 500;
            line-height: 1.3;
        }

        /* User Info */
        .info-grid {
            display: grid;
            grid-template-columns: 1fr;
            gap: 12px;
        }

        .info-item {
            display: flex;
            align-items: center;
            padding: 12px;
            border-radius: 6px;
            background: rgba(30, 41, 59, 0.5);
            border: 1px solid var(--border);
        }

        .info-icon {
            width: 32px;
            height: 32px;
            border-radius: 6px;
            background: rgba(14, 165, 233, 0.1);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--accent);
            margin-right: 12px;
            flex-shrink: 0;
            font-size: 0.9rem;
        }

        .info-content {
            flex: 1;
            min-width: 0;
        }

        .info-label {
            font-size: 0.75rem;
            color: var(--text-light);
            margin-bottom: 3px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .info-value {
            font-weight: 500;
            font-size: 0.85rem;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        /* Responsive */
        @media (min-width: 768px) {
            body {
                padding: 20px;
            }
            
            .dashboard-grid {
                grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
                gap: 20px;
            }
            
            .quick-actions-grid {
                grid-template-columns: repeat(4, 1fr);
            }
            
            .card {
                padding: 20px;
                min-height: 200px;
            }
            
            .stat-value {
                font-size: 2.5rem;
            }
        }

        @media (max-width: 480px) {
            .dashboard-header {
                padding: 10px 0;
                margin-bottom: 15px;
            }
            
            .header-left h1 {
                font-size: 1.2rem;
            }
            
            .avatar {
                width: 30px;
                height: 30px;
                font-size: 0.9rem;
            }
            
            .user-name {
                font-size: 0.8rem;
            }
            
            .user-role {
                font-size: 0.65rem;
            }
            
            .card-title {
                font-size: 1rem;
            }
            
            .card-icon {
                width: 28px;
                height: 28px;
                font-size: 0.9rem;
            }
            
            .stat-value {
                font-size: 2rem;
                margin: 8px 0;
            }
            
            .stat-label {
                font-size: 0.8rem;
            }
            
            .quick-action {
                padding: 12px 8px;
                min-height: 80px;
            }
            
            .action-icon {
                font-size: 1.2rem;
            }
            
            .action-label {
                font-size: 0.7rem;
            }
        }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <!-- Header -->
        <header class="dashboard-header">
            <div class="header-left">
                <h1>Tableau de bord</h1>
            </div>
            <div class="user-profile">
                <div class="avatar"><%= user.username.charAt(0).toUpperCase() %></div>
                <div class="user-info">
                    <div class="user-name"><%= user.username %></div>
                    <div class="user-role">Membre Premium</div>
                </div>
            </div>
        </header>

        <!-- Main Grid -->
        <div class="dashboard-grid">
            <!-- IA Favorites -->
            <div class="card stat-card">
                <div class="card-header">
                    <h3 class="card-title">IA Favorites</h3>
                    <div class="card-icon">
                        <i class="fas fa-star"></i>
                    </div>
                </div>
                <div class="stat-value"><%= favoritesCount %></div>
                <div class="stat-label">IA dans vos favoris</div>
            </div>

            <!-- Commentaires -->
            <div class="card stat-card">
                <div class="card-header">
                    <h3 class="card-title">Commentaires</h3>
                    <div class="card-icon">
                        <i class="fas fa-comment"></i>
                    </div>
                </div>
                <div class="stat-value"><%= commentsCount %></div>
                <div class="stat-label">Commentaires postés</div>
            </div>

            <!-- Section accès rapide -->
            <div class="card" style="grid-column: span 2;">
                <div class="card-header">
                    <h3 class="card-title">Accès rapide</h3>
                    <div class="card-icon">
                        <i class="fas fa-bolt"></i>
                    </div>
                </div>
                <div class="quick-actions-grid">
                    <a href="/user/favorites" class="quick-action">
                        <div class="action-icon">
                            <i class="fas fa-star"></i>
                        </div>
                        <div class="action-label">Favoris</div>
                    </a>
                    <a href="/ai" class="quick-action">
                        <div class="action-icon">
                            <i class="fas fa-robot"></i>
                        </div>
                        <div class="action-label">Explorer les IA</div>
                    </a>
                    <a href="/prompts" class="quick-action">
                        <div class="action-icon">
                            <i class="fas fa-terminal"></i>
                        </div>
                        <div class="action-label">Générateur de prompts</div>
                    </a>
                    <a href="/" class="quick-action">
                        <div class="action-icon">
                            <i class="fas fa-cog"></i>
                        </div>
                        <div class="action-label">Retour a l'acceuil</div>
                    </a>
                </div>
            </div>

            <!-- User Info -->
            <div class="card" style="grid-column: span 2;">
                <div class="card-header">
                    <h3 class="card-title">Informations du compte</h3>
                    <div class="card-icon">
                        <i class="fas fa-user"></i>
                    </div>
                </div>
                <div class="info-grid">
                    <div class="info-item">
                        <div class="info-icon">
                            <i class="fas fa-user"></i>
                        </div>
                        <div class="info-content">
                            <div class="info-label">Nom d'utilisateur</div>
                            <div class="info-value"><%= user.username %></div>
                        </div>
                    </div>
                    <div class="info-item">
                        <div class="info-icon">
                            <i class="fas fa-envelope"></i>
                        </div>
                        <div class="info-content">
                            <div class="info-label">Email</div>
                            <div class="info-value"><%= user.email %></div>
                        </div>
                    </div>
                    <div class="info-item">
                        <div class="info-icon">
                            <i class="fas fa-calendar"></i>
                        </div>
                        <div class="info-content">
                            <div class="info-label">Membre depuis</div>
                            <div class="info-value"><%= new Date(user.createdAt).toLocaleDateString() %></div>
                        </div>
                    </div>
                    <div class="info-item">
                        <div class="info-icon">
                            <i class="fas fa-clock"></i>
                        </div>
                        <div class="info-content">
                            <div class="info-label">Dernière connexion</div>
                            <div class="info-value"><%= lastLogin %></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Animation pour les cartes
            const cards = document.querySelectorAll('.card');
            
            cards.forEach(card => {
                card.addEventListener('mouseenter', () => {
                    card.style.transform = 'translateY(-3px)';
                    card.style.boxShadow = '0 0 8px rgba(56, 189, 248, 0.3), 0 6px 15px rgba(0, 0, 0, 0.2)';
                });
                
                card.addEventListener('mouseleave', () => {
                    card.style.transform = 'translateY(0)';
                    card.style.boxShadow = '0 4px 15px rgba(0, 0, 0, 0.15)';
                });
            });
            
            // Animation pour les accès rapides
            const quickActions = document.querySelectorAll('.quick-action');
            
            quickActions.forEach(action => {
                action.addEventListener('mouseenter', () => {
                    action.style.transform = 'translateY(-3px)';
                    action.style.boxShadow = '0 4px 10px rgba(56, 189, 248, 0.2)';
                });
                
                action.addEventListener('mouseleave', () => {
                    action.style.transform = 'translateY(0)';
                    action.style.boxShadow = 'none';
                });
            });
        });
    </script>
</body>
</html>